<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>URL Shortener Project</title>
  <link rel="stylesheet" href="/styles.css">
  
</head>
<body>
  <form method="POST" action="/shorten">
    <div class="container">
    <h2>URL Shortener</h2>
    <input type="url" name = "longURL" id="longUrl" placeholder="Enter your long URL" >
    <button type="submit">Generate URL</button>
    <!-- <div class="output" id="shortUrl"></div> -->
  </div>

  </form>

  <h2>📄 All Shortened URLs</h2>
  <ul>
    <% for (let shortID in urls) { %>
      <li>
        <a href="/<%= shortID %>">http://localhost:8000/<%= shortID %></a> → 
        <span><%= urls[shortID] %></span>
      </li>
    <% } %>
  </ul>
  

  

</body>
</html>
